"""The ordered decision procedure that turns predictions into findings.

Filled at P3 (spec section 11.2). Compares declared against inferred labels, and
in corpus mode against the answer key. It never re-derives signals.
"""
