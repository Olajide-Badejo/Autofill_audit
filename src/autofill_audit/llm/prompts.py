"""The system prompt and the response JSON schema, both versioned.

Filled at P6 (spec section 12.2). Prompts are fed pruned field descriptors, never
raw HTML, which is what keeps the input small and the comparison fair.
"""
