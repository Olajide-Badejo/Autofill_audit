"""DOM traversal including same-origin frames and open shadow roots.

Filled at P2 (spec section 9.1). Closed shadow roots and cross-origin frames are
reported as undetectable rather than guessed at (spec section 9.6).
"""
