"""The machine-readable report at a versioned schema.

Filled at P3. Suppressed findings appear under their own key with a reason, so a
config file cannot make a finding vanish without trace (spec section 14).
"""
