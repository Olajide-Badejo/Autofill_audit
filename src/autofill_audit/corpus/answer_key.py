"""Answer-key emission and schema validation.

Filled at P1. Keys are separate JSON files, never ``data-truth`` attributes in
the generated HTML, so the extractor structurally cannot see them (spec 5.6).
"""
