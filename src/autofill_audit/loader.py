"""Playwright launch, navigation, and teardown with bounded waits.

Filled at P2. Uses the synchronous Playwright API for the reasons in spec
section 5.5. Never follows links, never submits, never types (spec section 0.4).
"""
