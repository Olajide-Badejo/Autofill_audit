"""The JSONL run-log writer at the schema of spec section 13.1.

Filled at P5. Run logs are written in the schema the external harness consumes
directly; they are never reshaped at the last moment to fit it (spec 0.5).
"""
