"""The only module permitted to import ``ml_experiment_triage``.

Filled at P5. Public API only: no monkey-patching, no subclassing around, no
reaching into internals. Insufficiency of the public API is a cross-repo task
recorded in ``docs/cross-repo-tasks.md``, never a local workaround (spec 0.5).
"""
