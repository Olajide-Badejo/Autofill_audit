"""Feature extraction shared by training and inference.

Filled at P4 (spec section 10.2). One implementation serves both paths so that a
training-time and inference-time featurisation cannot silently diverge.
"""
